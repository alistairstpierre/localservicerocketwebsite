# Ignores common local-only and generated files for this repo; expand when build tooling is added.

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Env / secrets (add patterns as needed)
.env
.env.local
.env.*.local

# Dependencies / build
node_modules/
dist/
.astro/

# Framework
.next/

# Stitch / design exports: local reference only (see README); not needed for production build
Stitch-Files/
